/**
 * @typedef {Object} Point
 * @property {number} x
 * @property {number} y
 */

/**
 * @typedef {Point} Vector
 */

/**
 * @typedef {string} Dimension Either 'width' or 'height'.
 */

/**
 * @typedef {Object} Dimensions
 * @property {number} width
 * @property {number} height
 */

/**
 * @typedef {Object} Rect
 * @property {number} x
 * @property {number} y
 * @property {number} width
 * @property {number} height
 */

/**
 * @typedef {Object} RectTRBL
 * @property {number} top
 * @property {number} right
 * @property {number} bottom
 * @property {number} left
 */

/**
 * @typedef {string} Axis Either 'x' or 'y'.
 */

/**
 * @typedef {string} Directions Either 'n', 'w', 's', 'e', 'nw', 'ne', 'sw' or 'se'.
 */

/**
 * @typedef {string} DirectionTRBL Either 'top', 'right', 'bottom', 'left', 'top-right', 'top-left', 'bottom-right' or 'bottom-left';
 */